require 'sinatra/base'
<% @routes.each do |r| %>require '<%= r.file_name %>'
<% end %>

class <%= app_name.classify %>App < Sinatra::Application
<% @routes.each do |r| %>  register Sinatra::<%= app_name.classify %>::Resources::<%= r.class_name %>
<% end %>
end
